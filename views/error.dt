extends layout

block title
  title PastaBin - Error

block content
  row#paste-infos
    div#size-wrapper
      column(cols="12")
        h1.text Error #{error.code} - #{error.message}

  row#about-wrapper
    div#about-content
      column(cols="12")
        p #{error.debugMessage}

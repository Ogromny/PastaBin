extends layout

block title
    title PastaBin - Encrypt

block content
    row
        column(cols="12").text-centered
            a.btn(href="/decrypt/#{hash}/#{password}/", type="primary", big) Decrypt Link
